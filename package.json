{
  "name": "flowfi",
  "version": "1.0.0",
  "description": "DeFi Payment Streaming on Stellar",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.3.2"
  },
  "lint-staged": {
    "frontend/**/*.{ts,tsx,js,jsx}": [
      "npm run lint --workspace=frontend --"
    ]
  }
}